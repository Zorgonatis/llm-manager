# Changelog

All notable changes to the LLM Management Solution will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed
- Replaced `build` config field with `binary` — now points directly to the `llama-server` executable instead of a build directory containing `bin/llama-server`
- Updated `models.conf` example with sections for CPU, Vulkan, CUDA, ROCm, multi-GPU, vision, and HuggingFace auto-download models

### Added
### Deprecated
### Removed
### Fixed
### Security

---
